export const loginUser = (
  credentials,
  navigate,
  setFieldError,
  setSubmitting
) => {
  const user = {
    name: "George",
    email: "cao@cadela.com",
  };
  const status = true;

  if (status === true) {
    //Allow access and redirect
  } else {
    //Return error to the user
  }
};
export const signupUser = (
  credentials,
  navigate,
  setFieldError,
  setSubmitting
) => {};
export const logoutUser = () => {};
